import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Brand ───────────────────────────────────────────
  static const Color accent = Color(0xFF7C6EFF);
  static const Color accentLight = Color(0xFF9D93FF);
  static const Color accentDark = Color(0xFF5A4EDD);
  static const Color green = Color(0xFF4CAF50);

  // ── Dark Theme ──────────────────────────────────────
  static const Color darkBg = Color(0xFF0E0E14);
  static const Color darkSurface = Color(0xFF1A1A24);
  static const Color darkCard = Color(0xFF1E1E2A);
  static const Color darkBorder = Color(0xFF2E2E3E);
  static const Color darkText = Color(0xFFF0F0F8);
  static const Color darkText2 = Color(0xFF9090A8);
  static const Color darkText3 = Color(0xFF606075);

  // ── Light Theme ─────────────────────────────────────
  static const Color lightBg = Color(0xFFF4F4F8);
  static const Color lightSurface = Color(0xFFFFFFFF);
  static const Color lightCard = Color(0xFFFFFFFF);
  static const Color lightBorder = Color(0xFFE0E0EC);
  static const Color lightText = Color(0xFF1A1A2E);
  static const Color lightText2 = Color(0xFF6060A0);
  static const Color lightText3 = Color(0xFFA0A0C0);

  // ── Luxury Theme ────────────────────────────────────
  static const Color luxBg = Color(0xFF0A0A0F);
  static const Color luxSurface = Color(0xFF141420);
  static const Color luxCard = Color(0xFF1C1C2C);
  static const Color luxBorder = Color(0xFF2A2A3C);
  static const Color luxAccent = Color(0xFFD4AF37);
  static const Color luxAccentLight = Color(0xFFFFD700);

  // ── Sports Theme ────────────────────────────────────
  static const Color sportsBg = Color(0xFF0D1117);
  static const Color sportsSurface = Color(0xFF161B22);
  static const Color sportsCard = Color(0xFF1C2128);
  static const Color sportsBorder = Color(0xFF30363D);
  static const Color sportsAccent = Color(0xFF00C853);

  // ── Semantic ─────────────────────────────────────────
  static const Color ok = Color(0xFF4CAF50);
  static const Color warn = Color(0xFFFFC107);
  static const Color error = Color(0xFFF44336);
  static const Color info = Color(0xFF2196F3);

  // ── Macros ───────────────────────────────────────────
  static const Color proteinColor = Color(0xFF4CAF50);
  static const Color carbsColor = Color(0xFF2196F3);
  static const Color fatColor = Color(0xFFFFC107);
  static const Color fiberColor = Color(0xFF9C27B0);
  static const Color waterColor = Color(0xFF00BCD4);

  // ── Evaluation ──────────────────────────────────────
  static const Color evalS1 = Color(0xFFFFD700);
  static const Color evalS2 = Color(0xFF00E676);
  static const Color evalS3 = Color(0xFF4CAF50);
  static const Color evalGd = Color(0xFF2196F3);
  static const Color evalSt = Color(0xFFFF9800);
  static const Color evalDn = Color(0xFFF44336);
  static const Color evalBeg = Color(0xFF9E9E9E);

  // ── Attendance ──────────────────────────────────────
  static const Color attGym = Color(0xFF4CAF50);
  static const Color attAbsent = Color(0xFFF44336);
  static const Color attRest = Color(0xFF9E9E9E);
}
